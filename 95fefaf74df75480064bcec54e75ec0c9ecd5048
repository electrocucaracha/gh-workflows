{"category": "Fixed", "changelog_entry": "Resolved runtime errors during CI runs by ensuring the workflow can locate and invoke the correct server through the addition of the required mcpServers key to the improvers workflow configuration.", "summary": "The workflow configuration for CI runs was updated to include the missing mcpServers key. This change is necessary for the graphify command to parse and execute correctly, preventing runtime errors. The addition of this key ensures the workflow can locate and invoke the correct server, resolving a critical configuration issue.", "version": 1}
