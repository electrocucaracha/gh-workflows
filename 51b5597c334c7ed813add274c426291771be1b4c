{"category": "Added", "changelog_entry": "Enabled authentication for Copilot in CI environments by requiring the COPILOT_TOKEN secret in both improvers and linter GitHub workflows.", "summary": "COPILOT_TOKEN is now a required secret in both improvers and linter GitHub workflows, enforcing authentication for Copilot and resolving usage issues in CI environments. This change affects the linter workflow, where the COPILOT_GITHUB_TOKEN is set to the secret when failures occur. The primary outcome is improved security and reduced errors in CI environments.", "version": 1}
