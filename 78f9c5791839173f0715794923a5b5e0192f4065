{"category": "Changed", "changelog_entry": "Eliminated dead configuration by removing stale GitHub Action exceptions from the ci/update_versions.sh script and deleting unused workflow diagram exports.", "summary": "The `ci/update_versions.sh` script was updated to remove stale GitHub Action exceptions and delete unused workflow diagram exports. This change eliminates dead configuration and reduces technical debt. No breaking behavior, API or CLI changes, or security impact were introduced, and no migration steps are required.", "version": 1}
