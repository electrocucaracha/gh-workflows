{"category": "Changed", "changelog_entry": "Improved the CI workflow for linter tasks by enabling consistent triggers, restricting Copilot CLI runs to merged pull requests, and streamlining graphify usage, thereby reducing the likelihood of issues on closed but unmerged pull requests.", "summary": "The CI workflow for linter tasks was updated to include all PR actions in the trigger, restrict Copilot CLI runs to merged PRs, and install graphify with the mcp extra. The graphify-mcp command is now used separately, and the mcpServers config was updated to call graphify-mcp with the generated output file. This change ensures consistency in the trigger and avoids filing issues on closed but unmerged PRs.", "version": 1}
