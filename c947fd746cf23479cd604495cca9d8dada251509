{"category": "Changed", "changelog_entry": "Modernized the linter workflow documentation to integrate with the Copilot CLI, replacing the `models: read` permission with the required `COPILOT_TOKEN` secret and clarifying secret and permission boundaries for the new analysis and reporting workflow steps.", "summary": "The linter workflow documentation has been updated to reflect the integration with the Copilot CLI. The `models: read` permission has been replaced with the required `COPILOT_TOKEN` secret, and unnecessary permission references have been removed. The documentation now explains how to initialize `rtk` and log Copilot CLI and `rtk` metrics to the Actions job summary.", "version": 1}
