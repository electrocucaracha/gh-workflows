{"category": "Changed", "changelog_entry": "Enabled the maintenance, update, and release workflows to be invoked as reusable workflows via `workflow_call`, improving modularity and enabling other workflows or repositories to programmatically reuse these automation paths.", "summary": "The maintenance and release workflows can now be invoked as reusable workflows via `workflow_call`, improving modularity and allowing other workflows or repositories to programmatically reuse these automation paths. The `improvers` and `update` workflows can be triggered on schedule or manual dispatch, and the `release` workflow can also be called as a reusable workflow. This change does not affect workflow logic or scheduling behavior.", "version": 1}
