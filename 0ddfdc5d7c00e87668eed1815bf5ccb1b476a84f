{"category": "Removed", "changelog_entry": "Enabled more accurate linting and formatting by removing unnecessary rule suppressions and updating configuration files to reflect the project's use of Semantic Line Breaks.", "summary": "This commit removes unnecessary rule suppressions and updates configuration files to match the installed CLI version. The changes include updating the biome schema, enabling VCS-aware ignore handling, and excluding generated analysis artifacts from formatting and linting. Additionally, YAML examples in documentation files are updated to avoid yamllint rule suppressions, and the markdownlint configuration is updated to reflect the project's use of Semantic Line Breaks.", "version": 1}
