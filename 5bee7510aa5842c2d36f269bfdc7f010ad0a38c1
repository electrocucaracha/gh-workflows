{"category": "Added", "changelog_entry": "Enabled explicit authentication for reusable workflows by requiring the WORKFLOW_TOKEN secret in the workflow_call configuration, thereby improving security and preventing accidental execution without proper credentials.", "summary": "The workflow_call trigger now requires a WORKFLOW_TOKEN secret to be provided, improving security by preventing accidental execution without proper credentials. This change enforces explicit authentication for reusable workflows. Existing callers must update their workflow_call usage to include this secret, which is now a required secret in the workflow_call configuration.", "version": 1}
